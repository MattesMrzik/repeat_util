name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make -C repeat_util/consecutive_kmers/cpp_code
    - name: make test
      run: make -C repeat_util/consecutive_kmers/cpp_code test
