Bootstrap: docker
From: ubuntu:latest

%post
    apt-get update && apt-get install -y g++ libhts-dev zlib1g-dev

%files
    bin/main /usr/local/bin/main
    bin/test /usr/local/bin/test
    test/resources/ /usr/local/test/resources


%test
    /usr/local/bin/main -h